#ifndef MODEL_DATA_LAYER_DATA
#define MODEL_DATA_LAYER_DATA

// This struct is empty by default, but if layers that needs to store data in
// the state can use a class that inherits from this struct.

struct layer_data_t {};

#endif
